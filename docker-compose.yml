version: '3.8'

services:
  db:
    image: postgres
    container_name: kittens_db
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
      POSTGRES_DB: kittens_db
    ports:
      - "5432:5432"
#    volumes:
#      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
